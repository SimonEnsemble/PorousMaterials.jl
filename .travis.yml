language: julia
script:
        julia -e 'Pkg.clone(pwd())'
        julia -e 'Pkg.build("PorousMaterials")'
        if [ -f test/runtests.jl ]; then
            cd test; julia 'Pkg.build("HDF5")'; julia 'runtests.jl'
        fi
