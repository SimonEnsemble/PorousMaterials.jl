#create dictionary for bonding rules


function find_bonds(function find_bonds(framework::Framework, atom_id::Int, bonding_rules)

#for all atoms in the unit cell

        #find distance between atom we care about and other atom

        #find what the atoms are so the bond length dict can be used

        #use the bond length dict to find the characteristic bond lenghth

        #if bond length is less than the characteristic bond length
                #add bond to list of bonds for the atom
        #end
#end
